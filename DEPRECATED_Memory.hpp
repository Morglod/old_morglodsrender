#pragma once
/*
#ifndef _MR_MEMORY_H_
#define _MR_MEMORY_H_

#include <stack>

namespace MR {
    class MemoryManager;

    class MemoryHandle {
    public:
        MemoryHandle();
        virtual ~MemoryHandle();
    };

    class MemoryManager {
    public:
        static MemoryManager Instance;
        void Register(MemoryHandle* mem);
        void DeleteAll();
        MemoryManager();
        ~MemoryManager();
    protected:
        std::stack<MemoryHandle*> memory;
    };
}

#endif // _MR_MEMORY_H_
*/
